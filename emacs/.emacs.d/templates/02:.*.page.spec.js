import { createVM, destroyVM } from '~test/unit/specs/utils';
$0

describe('', () => {
  let vm;

  afterEach(() => {
    destroy(vm);
  });

  function getRootElement() {
    return vm.$el;
  }

  beforeEach(() => {

  });

  it('renders', () => {

  });
});