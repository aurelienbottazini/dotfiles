name: dox
root: ~/projects/work/doximity

windows:
  - dev:
      layout: main-horizontal
      panes:
        - emacs
  - servers:
      layout: main-horizontal
      panes:
        - docker-compose up
        - git pull && bundle && bundle exec rake db:migrate && bundle exec rails s webrick -p 5000
  - activities:
      layout: main-horizontal
      panes:
        - cd ~/projects/work/activities ;and bundle exec rails s webrick -p 5150
